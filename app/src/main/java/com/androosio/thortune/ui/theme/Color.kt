package com.androosio.thortune.ui.theme

import androidx.compose.ui.graphics.Color

// "Thunder" palette — a charged blue spark against a storm-night, AMOLED-friendly ground.
// The accent is the one bold move; everything around it stays quiet steel and near-black.
val ThunderBlue = Color(0xFF7AA2FF)        // primary — the lightning spark
val ThunderBlueDeep = Color(0xFF1E2C52)    // primary container
val ThunderBlueOn = Color(0xFF06122C)      // on primary
val ThunderBlueOnContainer = Color(0xFFD7E2FF)

val StormSteel = Color(0xFFB4BAD0)         // secondary — cold steel
val StormSteelContainer = Color(0xFF2A2F3E)
val VioletSpark = Color(0xFFC9A6FF)        // tertiary — a violet edge to the bolt
val VioletSparkContainer = Color(0xFF332247)

val StormNight = Color(0xFF07080C)         // background / surface (near-black for OLED)
val StormSurfaceLow = Color(0xFF0D0F15)
val StormSurface = Color(0xFF121521)
val StormSurfaceHigh = Color(0xFF1A1E2C)
val StormSurfaceHighest = Color(0xFF222637)
val MistHigh = Color(0xFFE4E7F1)           // on surface
val MistLow = Color(0xFFAEB4C6)            // on surface variant
val StormOutline = Color(0xFF464B5E)
val StormOutlineVariant = Color(0xFF2C3142)
